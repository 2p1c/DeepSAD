# Data Agent Guide

## 目标
将原始一维超声信号转换为可训练样本。

## 必须完成
1. 滑动窗口切片（`window_size`, `stride`）。
2. 标准化（每条信号或全局，按配置实现）。
3. 数据划分必须按路径（`split_by: path`）。

## 注意事项
- 严禁 train/test 泄露。
- 训练集必须只包含健康数据。
- 样本需保留 `path_id`，供路径级聚合分析。
